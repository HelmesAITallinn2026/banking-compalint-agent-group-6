package com.bcag6.repository;

import com.bcag6.entity.RefusalReason;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RefusalReasonRepository extends JpaRepository<RefusalReason, Integer> {
}
